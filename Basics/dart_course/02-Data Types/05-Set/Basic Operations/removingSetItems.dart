main() {
  Set numbers = {1, 2, 3, 4, 5};
  print('The value of Set before removing the Set element ${numbers}');
  bool res = numbers.remove(1);
  print('The value of Set after removing the Set element ${numbers}');
}
